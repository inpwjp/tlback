language: ruby
services:
  - postgresql
rvm: 
  - 2.2.2

install:
  - bundle install
script:
  - docker-compose up -d 
  - bundle exec rake set_settings_from_env
  - bundle exec rake restore_schema
  - netstat -nl
  - bundle exec rake test
